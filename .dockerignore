# Build context only needs config/ -- the paths the Dockerfile COPYs. kit/ is
# applied by sbx at sandbox creation, not by this build, so it's excluded too.
#
# Allowlist ("*" + "!" exceptions) so a new COPY must explicitly widen this
# file instead of the rest of the repo riding along unnoticed.
*
!config
