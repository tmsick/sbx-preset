# The personal content a kit injects (kit/claude/'s CLAUDE.md, say). spec.yaml
# stays committed -- it's just the declarative wrapper -- but the files/ tree
# underneath it is per-user.
/kit/*/files/

# Image tarballs staged for `sbx template load`.
/tmp/

# Byte-compiled tools/ scripts.
__pycache__/
